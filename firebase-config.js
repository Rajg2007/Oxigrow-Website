/* firebase-config.js
  Replace the placeholders below with your Firebase project configuration if you want
  to enable Firestore order persistence and hosting. For the IA you can keep
  the config commented if not using Firebase.
*/

// Example structure (do NOT commit real credentials to public repos):
/*
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};

firebase.initializeApp(firebaseConfig);
const firestore = firebase.firestore();
*/
